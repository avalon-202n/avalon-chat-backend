version: "3.8"
services:
  redis:
    image: redis:alpine
    command: redis-server --requirepass avalon --port 6379
    container_name: redis
    hostname: avalon-chat
    labels:
      - "name=redis"
      - "mode=standalone"
    ports:
      - "6379:6379"
