spring:
  datasource:
    url: jdbc:h2:mem:avalondb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
